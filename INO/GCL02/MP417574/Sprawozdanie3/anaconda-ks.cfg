# Generated by Anaconda 41.35
# Generated by pykickstart v3.58
#version=DEVEL

# Keyboard layouts
keyboard --vckeymap=us --xlayouts='us'

# System language
lang en_US.UTF-8

# Network configuration
network --bootproto=dhcp --device=enp0s3 --hostname=fedora.test --ipv6=auto --activate

%post --log=/tmp/postinstall.log --interpreter /bin/bash
exec > /dev/tty3 2>&1
chvt 3
echo "Postinstall..."

mkdir -p /opt/example

chown aborek:aborek /opt/example

# Pobierz plik main.c z GitHuba
curl -o /opt/example/main.c      https://raw.githubusercontent.com/InzynieriaOprogramowaniaAGH/MDO2025_INO/refs/heads/MP417574/INO/GCL02/MP417574/Sprawozdanie3/Dockerfiles_p/main.c

# Skrypt uruchamiany po zalogowaniu 
cat << 'EOF' > /etc/profile.d/run_example.sh
#!/bin/bash
if [ ! -f /opt/example/.compiled ]; then
    echo "Kompilacja..." >> /opt/example/autostart.log
    gcc /opt/example/main.c -o /opt/example/example -lcjson -I/usr/local/include/cjson -L/usr/local/lib64
    if [ -f /opt/example/example ]; then
        echo "Uruchamianie programu..." >> /opt/example/autostart.log
        LD_LIBRARY_PATH=/usr/local/lib64 /opt/example/example >> /opt/example/autostart.log 2>&1
    else
        echo "Kompilacja nie powiodła się :<" >> /opt/example/autostart.log
    fi
    touch /opt/example/.compiled
fi
EOF

chmod +x /etc/profile.d/run_example.sh
%end

repo --name=myrepo --baseurl=http://192.168.0.7/myrepo/

%packages
@^custom-environment
cjson
gcc
glibc
curl
%end

# Run the Setup Agent on first boot
firstboot --enable

# Fedora default repos
url --mirrorlist=http://mirrors.fedoraproject.org/mirrorlist?repo=fedora-41&arch=x86_64
repo --name=update --mirrorlist=http://mirrors.fedoraproject.org/mirrorlist?repo=updates-released-f41&arch=x86_64

# Disk setup
ignoredisk --only-use=sda
clearpart --none --initlabel
autopart

# System timezone
timezone Europe/Warsaw --utc

# Users
rootpw --iscrypted --allow-ssh $y$j9T$eKgdXsLTmrSEuSPjMoYgokYv$EzoYQqbicne4onhOqzWeVa420g3u.59tCdlzzVbTSb8
user --groups=wheel --name=kickstart \
     --password=$y$j9T$eKgdXsLTmrSEuSPjMoYgokYv$EzoYQqbicne4onhOqzWeVa420g3u.59tCdlzzVbTSb8 \
     --iscrypted --gecos="kickstart"

reboot