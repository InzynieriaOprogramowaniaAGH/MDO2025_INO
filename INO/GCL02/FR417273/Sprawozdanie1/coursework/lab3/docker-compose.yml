services:
  cj-build:
    build:
      context: .
      dockerfile: Dockerfile.build
    container_name: cj-build

  cj-test:
    build:
      context: .
      dockerfile: Dockerfile.test
    container_name: cj-test
    depends_on:
      - cj-build
