# Generated by Anaconda 41.35
# Generated by pykickstart v3.58
#version=DEVEL

# Keyboard layouts
keyboard --vckeymap=pl --xlayouts='pl'
# System language
lang pl_PL.UTF-8

network --hostname=fedora.local

%post--log=/tmp/postinstall.log --interpreter /bin/bash
exec > /dev/tty3 2>&1
chvt 3
echo "Start postinstall"
mkdir -p /opt/example

chown abaca:abaca /opt/example

# Pobierz plik main.c z GitHuba
curl -o /opt/example/main.c https://raw.githubusercontent.com/InzynieriaOprogramowaniaAGH/MDO2025_INO/AB414799/INO/GCL01/AB414799/Sprawozdanie2/Complete_pipeline/main.c

# Skrypt uruchamiany po zalogowaniu (tekstowo)
cat << 'EOF' > /etc/profile.d/run_example.sh
#!/bin/bash
if [ ! -f /opt/example/.compiled ]; then
    echo "Kompilacja main.c" >> /opt/example/autostart.log
    gcc /opt/example/main.c -o /opt/example/example -lcjson -I/usr/local/include/cjson -L/usr/local/lib64
    if [ -f /opt/example/example ]; then
        echo "Uruchamienie:" >> /opt/example/autostart.log
        LD_LIBRARY_PATH=/usr/local/lib64 /opt/example/example >> /opt/example/autostart.log 2>&1
    else
        echo "Brak powodzenia" >> /opt/example/autostart.log
    fi
    touch /opt/example/.compiled
fi
EOF

chmod +x /etc/profile.d/run_example.sh
%end

repo --name=myrepo --baseurl=http://localhost:8080/myrepo/

%packages
@^custom-environment
cjson
gcc
glibc
curl
%end

# Run the Setup Agent on first boot
firstboot --enable

url --mirrorlist=http://mirrors.fedoraproject.org/mirrorlist?repo=fedora-41&arch=x86_64
repo --name=update --mirrorlist=http://mirrors.fedoraproject.org/mirrorlist?repo=updates-released-f41&arch=x86_64

# Generated using Blivet version 3.11.0
ignoredisk --only-use=sda

# Partition clearing information
bootloader --location=mbr --boot-drive=sda
clearpart --all --initlabel
autopart

# System timezone
timezone Europe/Warsaw --utc

# Root password
rootpw --iscrypted $y$j9T$0WVhbTMynFK7Ch2J4cXp7RHK$0T566W8cFj/y4dJSH0iKgzTho6BmatrkTlMhJtnMoJ3
user --groups=wheel --name=abaca --gecos="Adrian Baca" --password=fedorka123 --plaintext

reboot