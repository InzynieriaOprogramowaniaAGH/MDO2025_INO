apiVersion: v1
kind: Service
metadata:
  name: nginx-service
spec:
  selector:
    app: nginx   # Wybiera pody z obu deploymentów (stable i canary)
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  sessionAffinity: None