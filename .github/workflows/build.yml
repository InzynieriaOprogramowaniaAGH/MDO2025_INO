name: Node.js Build

on:
  push:
    branches:
      - ino_dev

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Install dependencies
        run: npm install
        working-directory: ITE/GCL06/JP416100/Sprawozdanie2/node-js-dummy-test

      - name: Build project
        run: npm run build
        working-directory: ITE/GCL06/JP416100/Sprawozdanie2/node-js-dummy-test

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: build-output
          path: ITE/GCL06/JP416100/Sprawozdanie2/node-js-dummy-test/dist

