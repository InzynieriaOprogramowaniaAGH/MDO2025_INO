apiVersion: v1
kind: Pod
metadata:
  name: nginx-pod
  labels:
    app: nginx
spec:
  containers:
    - name: nginx
      image: my-custom-nginx:latest
      imagePullPolicy: Never
      ports:
        - containerPort: 8080

