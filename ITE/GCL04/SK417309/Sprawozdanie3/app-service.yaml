apiVersion: v1
kind: Service
metadata:
  name: exam-seat-service
spec:
  type: NodePort
  selector:
    app: exam-seat-app
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30080