FROM eclipse-temurin:21-jdk-alpine

WORKDIR /app

COPY --from=builder /app/target/*.jar app.jar

ENTRYPOINT ["sh", "-c", "java -jar app.jar & sleep 10 && curl -f http://localhost:8081/actuator/health || exit 1"]
