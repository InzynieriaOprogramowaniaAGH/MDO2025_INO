apiVersion: v1
kind: Service
metadata:
  name: weatherforecast-api-canary-service
spec:
  type: NodePort
  selector:
    app: weatherforecast-api
  ports:
  - protocol: TCP
    port: 5000
    targetPort: 5000