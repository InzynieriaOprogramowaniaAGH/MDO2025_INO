all:
  children:
    Orchestrators:
      hosts:
        orchestrator:
          ansible_host: 127.0.0.1
          ansible_user: ansible

    Endpoints:
      hosts:
        ansible-target:
          ansible_host: 192.168.56.102
          ansible_user: ansible
        ansible-endpoint2:
          ansible_host: ansible-endpoint2
          ansible_user: ansible

