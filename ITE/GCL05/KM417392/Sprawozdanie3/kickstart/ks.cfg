# Generated by Anaconda 41.35
# Generated by pykickstart v3.58
#version=DEVEL

# Keyboard layouts
keyboard --vckeymap=pl --xlayouts='pl'
# System language
lang pl_PL.UTF-8

# Network information
network  --bootproto=dhcp --device=enp0s3 --ipv6=auto --activate
network  --hostname=fedora-mruby.local

# System timezone
timezone Europe/Warsaw --utc

# Root user and regular user
rootpw haslo123 --plaintext
user --groups=wheel --name=kasiam --password=haslo123 --plaintext --gecos="kasiam"

# Partitioning
ignoredisk --only-use=sda
autopart
clearpart --none --initlabel

# Software selection
%packages
@^server-product-environment
docker
curl
%end

# Enable first boot setup agent
firstboot --enable

# Repositories
url --mirrorlist=http://mirrors.fedoraproject.org/mirrorlist?repo=fedora-41&arch=x86_64
repo --name=update --mirrorlist=http://mirrors.fedoraproject.org/mirrorlist?repo=updates-released-f41&arch=x86_64

# %post section for Docker and service setup
%post --log=/root/post-install.log

# Enable Docker service at boot (start będzie działał dopiero po restarcie)
systemctl enable docker

# Dodanie użytkownika 'kasiam' do grupy docker
usermod -aG docker kasiam

# Utwórz systemd service do uruchamiania kontenera po starcie
cat > /etc/systemd/system/mruby-container.service << 'EOF'
[Unit]
Description=Run mruby container at startup
After=docker.service
Requires=docker.service

[Service]
Restart=always
ExecStart=/usr/bin/docker run --rm --name mruby-app -v /opt/script.rb:/app/script.rb kasiam23/mruby
ExecStop=/usr/bin/docker stop mruby-app

[Install]
WantedBy=multi-user.target
EOF

# Pobierz skrypt Ruby do /opt (zakładamy, że jest prosty)
mkdir -p /opt
echo 'puts "Hello world"' > /opt/script.rb

# Włącz naszą usługę
systemctl enable mruby-container.service

%end


reboot
