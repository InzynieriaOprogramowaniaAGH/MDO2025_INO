apiVersion: v1
kind: Service
metadata:
  name: moja-nginx-app-service
  labels:
    app: moja-nginx-app
spec:
  type: ClusterIP
  selector:
    app: moja-nginx-app
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP