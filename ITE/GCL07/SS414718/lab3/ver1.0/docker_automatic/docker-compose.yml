version: '3'
services:
  builder:
    build:
      context: .
      dockerfile: Dockerfile.build
    container_name: app_build1
  tester:
    build:
      context: .
      dockerfile: Dockerfile.test
    container_name: app_test1