# Generated by Anaconda 41.35
# Generated by pykickstart v3.58
#version=DEVEL
graphical

url --mirrorlist=http://mirrors.fedoraproject.org/mirrorlist?repo=fedora-41&arch=x86_64
repo --name="updates" --mirrorlist=http://mirrors.fedoraproject.org/mirrorlist?repo=updates-released-f41&arch=x86_64

# Keyboard layouts
keyboard --vckeymap=pl --xlayouts='pl'
# System language
lang pl_PL.UTF-8

# Network information
network  --bootproto=dhcp --device=enp0s3 --ipv6=auto --activate
network  --hostname=fedoraKickstart

%packages
@^server-product-environment
@admin-tools
@cloud-infrastructure
@text-internet
moby-engine
nodejs
git
%end

# Run the Setup Agent on first boot
firstboot --enable

# System timezone
timezone Europe/Warsaw --utc

# Root password
rootpw --iscrypted --allow-ssh $y$j9T$QUgwkqIjrFZKBDCxxafxwoj4$qYI0Zj4/d.Hi2WqZ3rE2xE8U8UawsyJE9CTJQoJXYC0
user --groups=wheel --name=lwrobel --password=$y$j9T$lFbBtRaAMSrJp1z4hOzrDf9Q$fkS4zkAnqVTN6w063BSyY533yzgcj5TOfbr84laM9c3 --iscrypted --gecos="Lukasz Wróbel"

# Generated using Blivet version 3.11.0
ignoredisk --only-use=sda
clearpart --all --initlabel
autopart --type=lvm

reboot

%post --log=/root/kickstart-post.log

# Stwórz katalog aplikacji
mkdir -p /opt/devops_app

# Skrypt, który uruchomi `npm install`
cat << 'EOF' > /opt/devops_app/firstboot.sh
#!/bin/bash
cd /opt/devops_app
echo "Starting npm install..." >> /root/firstboot.log
/usr/bin/npm install devops_lukasz_wrobel >> /root/firstboot.log 2>&1
if [ $? -eq 0 ]; then
    echo "Package installed successfully." >> /root/firstboot.log
else
    echo "Package installation failed." >> /root/firstboot.log
fi

# Wyłącz usługę po uruchomieniu
systemctl disable devops-app-firstboot.service
rm -f /etc/systemd/system/devops-app-firstboot.service
rm -f /opt/devops_app/firstboot.sh
EOF

chmod +x /opt/devops_app/firstboot.sh

# Tworzenie usługi systemd
cat << 'EOF' > /etc/systemd/system/devops-app-firstboot.service
[Unit]
Description=Install devops_lukasz_wrobel on first boot
After=network.target

[Service]
Type=oneshot
ExecStart=/opt/devops_app/firstboot.sh
RemainAfterExit=true
StandardOutput=journal
StandardError=journal

[Install]
WantedBy=multi-user.target
EOF

# Włącz usługę
systemctl enable devops-app-firstboot.service

%end