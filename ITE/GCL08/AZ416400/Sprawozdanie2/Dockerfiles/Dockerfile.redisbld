FROM fedora

RUN dnf -y install \
    git \
    which\
    python3 \
    make \
    gcc \
    g++ \
    gawk \
    jemalloc-devel 

RUN git clone https://github.com/redis/redis.git

WORKDIR /redis/deps
RUN make jemalloc

WORKDIR /redis
RUN make

