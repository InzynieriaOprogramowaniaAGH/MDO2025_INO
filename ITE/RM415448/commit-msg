commit_message=$(head -n 1 "$1")

pattern="^RM415448"

if [[ ! $commit_message =~ $pattern ]]; then
  echo "Błąd: commit message musi zaczynać się od: 'RM415448'"
  echo "Przykład poprawnego opisu: 'RM415448 _rafałPassing_ script update'"
  exit 1
fi

exit 0