# Użycie oficjalnego obrazu Jenkins
FROM jenkins/jenkins:lts

# Zmiana na użytkownika root, aby móc instalować Dockera
USER root

# Instalacja Dockera
RUN apt-get update && apt-get install -y docker.io

# Powrót do użytkownika Jenkins
USER jenkins